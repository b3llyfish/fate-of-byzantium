## populist
# county_create_score base score = -160
admin_gov_populist_county_create_score = 40
# county_join_score base score = -120
admin_gov_populist_county_join_score = 30
# ai_join_score base score = -25
admin_gov_populist_ai_join_score = 50

governor_efficiency_score = {
	value = 0
	# get governor efficiency (between -100 & 50)
	add = governor_efficiency_presented
	# range now -200 to 100
	multiply = 2
	# apply phase multipliers
	if = {
		limit = {
			struggle:fate_of_byzantium ?= {
				has_struggle_phase_parameter = gov_efficiency_increased_impact
			}
		}
		multiply = 1.5
		round = yes
	}
}